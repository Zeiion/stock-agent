# Stock Agent — Python backend dependencies (Python 3.10+)
#
# Required (the app will not start without these):
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
pandas==2.2.2
numpy==1.26.4
yfinance==0.2.40          # universal fallback data source for US / HK / CN + headlines (news)
tzdata==2024.1            # IANA timezones for market-hours logic (needed on Windows/slim images)

# Optional but recommended (the app degrades gracefully if missing):
#   akshare  -> A-share near-real-time quotes (no API key) + HK delayed data
#               + A股/港股 中文名搜索 (search) + A股头条 (news)
#   anthropic-> Anthropic Messages API tier (only used when ANTHROPIC_API_KEY is set;
#               the `claude` / `codex` CLI tiers do NOT need this package)
akshare==1.13.0
anthropic==0.34.0

# Test dependencies (only needed to run the suite: cd backend && python -m pytest):
pytest==8.2.2
pytest-asyncio==0.23.7
